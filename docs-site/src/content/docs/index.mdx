---
title: TTDash documentation
description: Local-first cost, token, request, model, and provider analytics for toktrack usage data.
template: splash
hero:
  title: Understand every token.
  tagline: TTDash turns local toktrack usage data into clear cost, request, cache, model, and provider insights—without sending your data to a hosted TTDash backend.
  actions:
    - text: Get started
      link: /ttdash/getting-started/
      icon: right-arrow
    - text: View on GitHub
      link: https://github.com/roastcodes/ttdash
      icon: external
      variant: minimal
---

import { Card, CardGrid, LinkCard } from '@astrojs/starlight/components'
import VersionBadge from '../../components/VersionBadge.astro'

export const base = `${import.meta.env.BASE_URL.replace(/\/$/, '')}/`

<VersionBadge />

<CardGrid>
  <Card title="Local-first by design" icon="padlock">
    Usage data, settings, and backups stay on the machine running TTDash. There is no hosted TTDash
    account, database, analytics, or tracking.
  </Card>
  <Card title="Analysis that stays useful" icon="analytics">
    Explore costs, tokens, requests, cache efficiency, forecasts, anomalies, limits, model mix, and
    provider concentration.
  </Card>
  <Card title="Fast paths for real workflows" icon="rocket">
    Import directly from toktrack, upload JSON, filter and drill down, then export the exact view as
    CSV or a localized PDF.
  </Card>
</CardGrid>

<figure class="dashboard-preview">
  <img
    src={`${base}screenshots/ttdash-dashboard.png`}
    alt="TTDash dashboard showing usage metrics, cost charts, and provider analysis"
    width="1600"
    height="1400"
    loading="eager"
    decoding="async"
  />
  <figcaption>Your local usage data, from headline metrics to detailed trends.</figcaption>
</figure>

## Start in minutes

Run the latest release without installing it globally:

```bash
npx --yes @roastcodes/ttdash@latest
```

TTDash opens a local, authenticated dashboard. Choose **Auto import** in the first-run view to collect compatible toktrack data, or upload a JSON export yourself.

<CardGrid>
  <LinkCard
    title="Install and run"
    description="Requirements, package runners, first launch, and local authentication."
    href="/ttdash/getting-started/"
  />
  <LinkCard
    title="Import usage data"
    description="Auto-import, JSON replacement uploads, and conservative backup merges."
    href="/ttdash/getting-started/importing-data/"
  />
  <LinkCard
    title="Configure deployment"
    description="CLI flags, storage paths, remote access, security, and Docker."
    href="/ttdash/deploying/configuration/"
  />
  <LinkCard
    title="Automate with the API"
    description="Authentication, JSON endpoints, error rules, and Server-Sent Events."
    href="/ttdash/reference/http-api/"
  />
</CardGrid>

## Privacy without surprises

TTDash serves the app and API from your own process. It contacts the npm registry only when it needs the pinned toktrack package or checks the latest toktrack version. Remote access is opt-in and always requires authentication.

Before exposing TTDash beyond loopback, read [Remote access and security](/ttdash/deploying/remote-access/) and the repository's [security policy](https://github.com/roastcodes/ttdash/security/policy).
